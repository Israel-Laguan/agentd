// Package memory documents institutional memory workflows.
//
// The durable repository currently lives in internal/kanban because memories are
// stored in the same SQLite database as projects, tasks, and events.
package memory
